<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parcelight">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainPage"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".LoginDM"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".HomepageDM"
            android:label="@string/app_name"
            >
        </activity>

        <activity
            android:name=".SignupDM"
            android:label="@string/app_name"
            >
        </activity>

        <activity
            android:name=".TrackingDM"
            android:label="@string/app_name"
            >
        </activity>

        <activity
            android:name=".SignupCust"
            android:label="@string/app_name"
            >
        </activity>

        <activity
            android:name=".DisplayItem"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ParcelDM"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".HomepageAdmin"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".AdminActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".KeyinItem"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".RecordDM"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ViewParcel"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ViewDM"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ListDMItem"
            android:label="@string/app_name">
        </activity>

        <activity
        android:name=".RecordItem"
        android:label="@string/app_name">
    </activity>

        <activity
            android:name=".ViewItem"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".DMNotes"
            android:label="@string/app_name">
        </activity>
    </application>

</manifest>